{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to dev (ts-node-dev)",
      "type": "node",
      "request": "attach",
      "port": 9229,
      "restart": true,
      "skipFiles": ["<node_internals>/**"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
